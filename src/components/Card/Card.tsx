import { Link } from "react-router-dom";

export const Card = () => (
  <>
    <Link to={"../"}>Back to breading page</Link>
    <br />
    <br />
    <span>Card (detail) page</span>
  </>
);
